const authController = require('./auth');
const userController = require('./user');
const postController = require('./post');

module.exports = {
  authController,
  userController,
  postController,
};
